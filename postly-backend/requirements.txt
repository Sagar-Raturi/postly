# Pinned to the versions this project was built and tested against
# (Python 3.14, Django 5.2 LTS).

# --- Core -------------------------------------------------------------------
Django==5.2.17
djangorestframework==3.18.1

# --- API plumbing -----------------------------------------------------------
django-cors-headers==4.9.0
django-filter==26.1

# --- Configuration ----------------------------------------------------------
django-environ==0.14.0

# --- Media ------------------------------------------------------------------
pillow==12.3.0

# --- Database ---------------------------------------------------------------
# Only needed when DATABASE_URL points at Postgres; harmless otherwise.
psycopg[binary]==3.3.5

# --- Tests ------------------------------------------------------------------
pytest==9.1.1
pytest-django==4.14.0
