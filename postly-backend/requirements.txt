# Pinned to the versions this project was built and tested against
# (Python 3.14, Django 5.2 LTS).

# --- Core -------------------------------------------------------------------
Django==5.2.17
djangorestframework==3.18.1

# --- API plumbing -----------------------------------------------------------
django-cors-headers==4.9.0
django-filter==26.1

# --- Authentication ---------------------------------------------------------
# allauth owns signup, email verification and password reset; dj-rest-auth
# exposes those flows as REST endpoints for the Next.js frontend.
django-allauth==65.19.3
dj-rest-auth==7.2.0
# Argon2 password hashing, in place of Django's default PBKDF2.
argon2-cffi==25.1.0
# No django-ratelimit: every dj-rest-auth view already carries a
# `throttle_scope`, so DRF's own ScopedRateThrottle covers brute-force
# protection without a further dependency. See REST_FRAMEWORK in settings.

# --- Configuration ----------------------------------------------------------
django-environ==0.14.0

# --- Media ------------------------------------------------------------------
pillow==12.3.0

# --- Content safety ---------------------------------------------------------
# Post bodies are HTML written by a person and rendered to other people on a
# Postly origin, so they are run through an allowlist on the way out of the
# public API. nh3 wraps Rust's ammonia: a real HTML parser rather than a
# regex. See blog/sanitize.py.
nh3==0.3.7

# --- Database ---------------------------------------------------------------
# Only needed when DATABASE_URL points at Postgres; harmless otherwise.
psycopg[binary]==3.3.5

# --- Tests ------------------------------------------------------------------
pytest==9.1.1
pytest-django==4.14.0
